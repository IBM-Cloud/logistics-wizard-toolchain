{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "OpenWhisk",
  "description": "OpenWhisk",
  "longDescription": "The recommendation service relies on OpenWhisk actions. To deploy these actions, the pipeline requires your OpenWhisk authorization key.",
  "type": "object",
  "properties": {
    "openwhisk_auth": {
      "type": "string"
    }
  },
  "required": [ ],
  "form": [
    {
      "type": "text",
      "readonly": false,
      "title": "OpenWhisk Authorization Key",
      "key": "openwhisk_auth"
    }
  ]
}
